# Python packages (install with: pip install -r requirements.txt)
# Note: LightGBM needs OpenMP at runtime. It is not a pip package:
#   macOS: brew install libomp
#   Windows: Build Tools for Visual Studio (C++) or conda install -c conda-forge libomp
#   Linux: apt install libomp (or libgomp) / yum install libomp
Flask==3.0.3
Werkzeug==3.0.4
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.4.2
lightgbm==4.5.0
joblib==1.4.2
reportlab==4.2.5
arabic-reshaper>=3.0.0
python-bidi>=0.4.2
gunicorn==23.0.0